# BuildKit per-Dockerfile ignore for docker/api.Dockerfile (paths are relative
# to the build context root). Keeps the context small and keeps secrets-shaped
# files and the harness out of the image context entirely.
**/node_modules
.git
**/.env*
.worktrees
loop/src
docs
**/*.md
**/dist
**/.turbo
